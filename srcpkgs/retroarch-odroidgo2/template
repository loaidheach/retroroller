# Template file for 'retroarch-odroidgo2'
pkgname=retroarch-odroidgo2
version=1.9.0
revision=24
_gitrev=f76d7df1950570fbd50565265111b36143c1f157
wrksrc="RetroArch-${_gitrev}"
build_style=configure
configure_args="--prefix=/usr
 --sysconfdir=/etc
 --disable-builtinminiupnpc
 --disable-caca
 --disable-git_version
 --disable-discord
 --disable-jack
 --disable-miniupnpc
 --disable-opengl
 --disable-opengl1
 --disable-pulse
 --disable-qt
 --disable-sdl
 --disable-sdl2
 --disable-libusb
 --disable-v4l2
 --disable-wayland
 --disable-x11
 --disable-vulkan
 --disable-xvideo
 --enable-networking
 --enable-ozone
 --enable-tinyalsa
 --enable-alsa
 --enable-egl
 --enable-freetype
 --enable-kms
 --enable-odroidgo2
 --enable-opengles
 --enable-opengles3
 --enable-udev
 --enable-wifi
"
conf_files="/etc/retroarch.cfg"
hostmakedepends="pkg-config unzip git rsync openssh"
makedepends="zlib-devel libxml2-devel freetype-devel libxkbcommon-devel
 librga-devel alsa-lib-devel eudev-libudev-devel libdrm-devel MesaLib-devel ffmpeg-devel"
short_desc="Official reference frontend for the libretro API"
maintainer="valadaa48 <valadaa48@gmx.com>"
license="GPL-3.0-or-later"
homepage="http://www.libretro.com/"
distfiles="https://github.com/valadaa48/RetroArch/archive/${_gitrev}.tar.gz"
checksum=903dfcff548a06f0379fe18ec164a0351bfd8eed00fe9147147a5fb8d45e3f6b
patch_args="-Np1"

do_configure() {
	if [ "$CROSS_BUILD" ]; then
		configure_args+=" --host=${XBPS_CROSS_TRIPLET}"
	fi

	case "$XBPS_TARGET_MACHINE" in
		armv7*)
            configure_args+=" --enable-neon"
            ;;
    esac
	
    export CFLAGS="$CFLAGS -O3 -fno-tree-vectorize"
    export ASFLAGS="$CFLAGS"
	./configure ${configure_args}
}

pre_build() {
	vsed -i gfx/drivers_context/drm_go2_ctx.c -e "s:<drm/drm_fourcc.h>:<libdrm/drm_fourcc.h>:" 
	vsed -i deps/libgo2/src/display.c -e "s:<drm/drm_fourcc.h>:<libdrm/drm_fourcc.h>:" 
	vsed -i gfx/drivers/oga_gfx.c -e "s:<drm/drm_fourcc.h>:<libdrm/drm_fourcc.h>:" 
    vsed -i menu/drivers/ozone/ozone.c -e "s/scale_factor = ozone->last_scale_factor/scale_factor = ozone->last_scale_factor \* 1.5/"
}

do_build() {
#    local _opts=""

#	case "$XBPS_TARGET_MACHINE" in
#		armv7*) _opts+="NEON_CFLAGS='-mfpu=neon-vfpv4 -mfloat-abi=hard' NEON_ASFLAGS='-mfpu=neon-vfpv4 -mfloat-abi=hard'";;
#    esac

    make V=1 -j$(nproc)
}

post_install() {
    vinstall ${FILESDIR}/retroarch.cfg 644 etc
    vsv retroarch
}

